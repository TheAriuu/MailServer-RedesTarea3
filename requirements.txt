# requirements.txt — Dependencias del mail server
# Se corre así: pip install -r requirements.txt
# También se requiere instalar: sudo apt install openssl python3-openssl

# Core framework
Twisted[tls,conch,smtp,pop3,jabber]>=23.0.0

# Zope interface (Twisted dependency)
zope.interface>=6.0

# pyOpenSSL for TLS support
pyOpenSSL>=23.0.0

# Service identity for certificate validation
service-identity>=23.0.0

# Cryptography backend
cryptography>=41.0.0
